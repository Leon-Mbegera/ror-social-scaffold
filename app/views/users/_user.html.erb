<li>
  Name: <%= user.name %>
  <span class="profile-link">
    <%= link_to 'See Profile',  user_path(user), class: 'profile-link' %>
  </span>
  <span>
    <%= link_to "Invite", friendships_path(friendship: {friend_id:@user.id, confirmed: true}), method: 'POST' %>
  </span>
</li>